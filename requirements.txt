# Queens Football CV Analysis dependencies
# Tested on Python 3.10+ on Apple Silicon (M4) with MPS acceleration.

# Core CV / video
opencv-python>=4.8.0,<5.0.0
numpy>=1.24.0,<2.0.0

# YOLOv8 + ByteTrack
ultralytics>=8.1.0,<9.0.0

# PyTorch with MPS support (Apple Silicon)
# If pip resolves the wrong wheel, install manually:
#   pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cpu
torch>=2.1.0
torchvision>=0.16.0

# Data / IO
pandas>=2.0.0,<3.0.0
openpyxl>=3.1.0,<4.0.0

# Utilities
scipy>=1.11.0,<2.0.0
